#include<stdio.h>

int main(){
    int a = 6; 
    int b = 3;
    int c = a + b;
    printf("The value of a is %d and Value of b is %d sum is %d\n" , a, b, c);
    // Modulus operator is used to get the remainder
    printf("The reminder when a is divided by b is: %d\n" , a%b);
    return 0;
 }